# Optional: PostgreSQL connection for server-side persistence
# If not set, the app works fine using client-side localStorage only
DATABASE_URL=postgresql://user:password@localhost:5432/backlinks

# Server port (default: 3001)
PORT=3001
